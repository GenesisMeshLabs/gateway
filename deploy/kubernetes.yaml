apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: genesis-mesh-gateway
spec:
  replicas: 2
  serviceName: genesis-mesh-gateway
  minReadySeconds: 10
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels: {app: genesis-mesh-gateway}
  template:
    metadata:
      labels: {app: genesis-mesh-gateway}
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 40
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels: {app: genesis-mesh-gateway}
          minDomains: 2
      containers:
        - name: gateway
          # Replace with your registry digest before applying.
          image: registry.example.org/genesis-mesh-gateway:REPLACE_WITH_DIGEST
          ports: [{name: http, containerPort: 8080}]
          env:
            - {name: GATEWAY_ADDR, value: "0.0.0.0:8080"}
            - {name: GATEWAY_POLICY_FILE, value: /run/gateway/policy.json}
            - {name: GATEWAY_STATE_FILE, value: /var/lib/gateway/state.db}
            - {name: GATEWAY_REDIS_URL_FILE, value: /run/gateway/redis.url}
            - {name: GATEWAY_QUOTA_NAMESPACE, value: organization-production}
            - {name: GATEWAY_MAX_INFLIGHT, value: "32"}
            - {name: GATEWAY_CPU_WORKERS, value: "2"}
            - {name: GATEWAY_MAX_BATCH_JOBS, value: "1"}
            - {name: GATEWAY_MAX_BATCH, value: "128"}
            - {name: TOKIO_WORKER_THREADS, value: "2"}
            - {name: TOKIO_MAX_BLOCKING_THREADS, value: "16"}
            - {name: RUST_LOG, value: info}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          resources:
            requests: {cpu: "1", memory: 128Mi}
            limits: {cpu: "2", memory: 512Mi}
          startupProbe:
            httpGet: {path: /health, port: http}
            periodSeconds: 2
            failureThreshold: 30
          livenessProbe:
            httpGet: {path: /health, port: http}
            periodSeconds: 10
          readinessProbe:
            httpGet: {path: /ready, port: http}
            periodSeconds: 3
          volumeMounts:
            - {name: policy, mountPath: /run/gateway, readOnly: true}
            - {name: state, mountPath: /var/lib/gateway}
      volumes:
        - name: policy
          secret:
            secretName: gateway-policy
  # Initialize each new PVC explicitly with --init-state using the approved
  # image and policy before starting its replica. Never auto-create lost state.
  volumeClaimTemplates:
    - metadata:
        name: state
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: 4Gi
---
apiVersion: v1
kind: Service
metadata:
  name: genesis-mesh-gateway
spec:
  selector: {app: genesis-mesh-gateway}
  ports: [{name: http, port: 8080, targetPort: http}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: genesis-mesh-gateway
spec:
  minAvailable: 1
  selector:
    matchLabels: {app: genesis-mesh-gateway}
